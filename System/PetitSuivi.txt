

Relire la suite.

Tester et commiter ces deux versions tout est codé et flashé (salon)
Version 1.04 SGw 
Version 1.01 RmDv, tester et faire un commit

TEST :
pb mise à l'heure pas assez dynamique, il manque 2 ou 3 sec entre HMI et sys... a creuser
comprend pas... 115000 Bds ? pb de timing dans la boucle 1 sec ? délay ?


RmDv corriger le no cmde ... 

Faire un txt de release de tous les 3 soft.



faire le tri dans infoLCD A FAIRE
faire une fonction qui gère l'affichage time, inutilement répété dans InfoLCd.c A FAIRE

LCD gestion RmDv à finir + SysML + doc HMI ou Sgw A FAIRE


HMI préévoir un bouton qui commute en fullscreen en cas de plantage A FAIRE
HMI retourner l'écran A FAIRE
HMI repréciser les heures, ce ne sont pas les même sur SGw et HMI (param prog)

!!! pb latence RmDv qd HMI fonctionne : qd perte de connection onduleur ! sinon OK
!!! attention l'HMI n'envoie pas des mise à jour régulière il faut forcer . Donc HMI à  revoir 
à ce niveau

ajouter sysML ds timeStampManagement 


int TimeStamp_GetTimeStampDeltaStamp(void)

void TimeStamp_SetTimeStampDeltaStamp(int delta)

Ajouter dépendance #include "DataFromHMI.h" pour infolCD


Préciser explication globale dans RmDv notamment, qu'est ce qui est communiqué
par exemple, les RmDv ne travaillent que sur 24h et reçoivent uniquement des durée, pas de date 
embarquée.

